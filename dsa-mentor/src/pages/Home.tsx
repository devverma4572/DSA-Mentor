import { Flame, Search, Target, Trophy } from 'lucide-react'
import { useMemo, useState } from 'react'
import { ProblemCard } from '../components/ProblemCard'
import { problems } from '../data/problems'
import type { Difficulty, Problem } from '../types'

interface HomeProps { onSelect: (problem: Problem) => void }
const topics = ['All', 'Arrays', 'Strings', 'Linked List', 'Stack', 'Queue', 'Binary Search', 'Trees', 'Graphs', 'Dynamic Programming']
const difficulties: (Difficulty | 'All')[] = ['All', 'Easy', 'Medium', 'Hard']

export function Home({ onSelect }: HomeProps) {
  const [search, setSearch] = useState(''); const [topic, setTopic] = useState('All'); const [difficulty, setDifficulty] = useState<Difficulty | 'All'>('All')
  const filtered = useMemo(() => problems.filter((problem) => (topic === 'All' || problem.topic === topic) && (difficulty === 'All' || problem.difficulty === difficulty) && `${problem.title} ${problem.description}`.toLowerCase().includes(search.toLowerCase())), [search, topic, difficulty])
  return <main className="mx-auto max-w-[1440px] px-5 pb-16 pt-8 lg:px-8 lg:pt-12"><section className="relative overflow-hidden rounded-3xl border border-white/[0.08] bg-[#101722] px-6 py-10 sm:px-10 lg:px-14 lg:py-14"><div className="absolute right-[-10%] top-[-70%] size-[520px] rounded-full border border-[#55d6be]/10" /><div className="absolute right-[8%] top-[-44%] size-[350px] rounded-full border border-[#55d6be]/10" /><div className="relative max-w-2xl"><p className="flex items-center gap-2 text-xs font-bold uppercase tracking-[0.2em] text-[#55d6be]"><span className="size-1.5 rounded-full bg-[#55d6be]" />Your thinking partner for DSA</p><h1 className="mt-5 text-4xl font-bold tracking-[-0.04em] text-white sm:text-5xl lg:text-6xl">Master DSA with an <span className="text-[#8de8d7]">AI Mentor.</span></h1><p className="mt-5 max-w-xl text-base leading-7 text-slate-400 sm:text-lg">Learn to solve problems step by step. Get hints, not handouts, and build the instincts that last beyond one solution.</p><button onClick={() => onSelect(problems[0])} className="mt-8 rounded-xl bg-[#55d6be] px-5 py-3 text-sm font-bold text-[#071016] transition hover:bg-[#8de8d7]">Start solving <span className="ml-2">→</span></button></div><div className="relative mt-10 grid max-w-2xl grid-cols-3 gap-3 border-t border-white/[0.08] pt-6 sm:gap-8"><div><p className="text-2xl font-bold text-white">12</p><p className="mt-1 text-xs text-slate-500">Problems solved</p></div><div><p className="flex items-center gap-2 text-2xl font-bold text-white">4 <Flame size={18} className="text-[#f1b962]" /></p><p className="mt-1 text-xs text-slate-500">Day streak</p></div><div><p className="text-2xl font-bold text-white">78%</p><p className="mt-1 text-xs text-slate-500">Accuracy</p></div></div></section><section className="mt-12"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-[#55d6be]">Practice library</p><h2 className="mt-2 text-2xl font-bold tracking-tight text-white">Choose your next challenge</h2></div><p className="text-sm text-slate-500">{filtered.length} of {problems.length} problems</p></div><div className="mt-6 flex flex-col gap-3 lg:flex-row"><label className="relative flex-1"><Search size={17} className="absolute left-3.5 top-3.5 text-slate-500" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search problems..." className="h-11 w-full rounded-xl border border-white/10 bg-[#101722] pl-10 pr-4 text-sm text-white outline-none transition placeholder:text-slate-600 focus:border-[#55d6be]/60" /></label><div className="flex gap-2 overflow-x-auto pb-1">{difficulties.map((item) => <button key={item} onClick={() => setDifficulty(item)} className={`whitespace-nowrap rounded-xl border px-4 py-2 text-xs font-semibold transition ${difficulty === item ? 'border-[#55d6be]/40 bg-[#55d6be]/10 text-[#8de8d7]' : 'border-white/10 bg-[#101722] text-slate-500 hover:text-slate-200'}`}>{item}</button>)}</div></div><div className="mt-3 flex gap-2 overflow-x-auto pb-2">{topics.map((item) => <button key={item} onClick={() => setTopic(item)} className={`whitespace-nowrap rounded-lg px-3 py-2 text-xs font-medium transition ${topic === item ? 'bg-white/[0.1] text-white' : 'text-slate-500 hover:bg-white/[0.04] hover:text-slate-200'}`}>{item}</button>)}</div>{filtered.length ? <div className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{filtered.map((problem, index) => <div key={problem.id} className="animate-rise-in" style={{ animationDelay: `${index * 35}ms` }}><ProblemCard problem={problem} onSelect={() => onSelect(problem)} /></div>)}</div> : <div className="mt-5 rounded-2xl border border-dashed border-white/10 bg-[#101722] px-6 py-16 text-center"><Target className="mx-auto text-slate-600" size={28} /><h3 className="mt-4 font-semibold text-white">No challenges found</h3><p className="mt-2 text-sm text-slate-500">Try a different search or clear one of the filters.</p></div>}</section><section className="mt-12 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-white/[0.08] bg-[#101722] p-5"><Trophy className="text-[#f1b962]" size={20} /><p className="mt-4 text-sm font-semibold text-white">Build your streak</p><p className="mt-1 text-xs leading-5 text-slate-500">A few focused minutes every day compounds quickly.</p></div><div className="rounded-2xl border border-white/[0.08] bg-[#101722] p-5"><Target className="text-[#55d6be]" size={20} /><p className="mt-4 text-sm font-semibold text-white">Think before you code</p><p className="mt-1 text-xs leading-5 text-slate-500">Your mentor will ask the next useful question.</p></div><div className="rounded-2xl border border-white/[0.08] bg-[#101722] p-5"><Flame className="text-[#f26d78]" size={20} /><p className="mt-4 text-sm font-semibold text-white">Learn from attempts</p><p className="mt-1 text-xs leading-5 text-slate-500">Feedback turns every run into a better mental model.</p></div></section></main>
}
